this runs the Fiji and makes it find the SimViewer plugin including all its dependencies
$ ./ImageJ-macosx  --jarpath ~/devel/EmbryoGen/SimViewer/target/dependency__OLD/  --jarpath ~/devel/EmbryoGen/SimViewer/target/

the dependencies can be collcted with:
$ mvn dependency:copy-dependencies
