Geometry
    remove <list>

Cmake 
    fix build chain if i3d in different path

Agents
    Create serialization and deserealization
    (Done: ShadowAgent, AbstractAgent, NucleusAgent, NucleusNSAgent, Nucleus4SAgent, ShapeHinter)
    (Missing: TrajectoriesHinter)